buildNumber=1
timestamp=1

# >>> Set by Maven
project.version=${project.version}
project.buildNumber=${buildNumber}
project.timestamp=${timestamp}
# <<<

db.health.check.statement=SELECT 1 FROM DUAL

rest.recursive.host=localhost
rest.recursive.port=8081
rest.recursive.path=/jee_reference_project/rest/person/data/

drools.resource.scanner.interval=10
